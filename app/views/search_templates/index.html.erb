<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All searches
    </h1>

    <a href="/searches/new" class="btn btn-block btn-outline-secondary">
      Add a new search
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "search_templates/searches_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          City
        </th>

        <th>
          Start date
        </th>

        <th>
          End date
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @searches.each do |search| %>
      <tr>
        <!-- Display search.id start -->
        <td>
          <%= search.id %>
        </td>
        <!-- Display search.id end -->

        <!-- Display search.user_id start -->
        <td>
          <%= search.user_id %>
        </td>
        <!-- Display search.user_id end -->

        <!-- Display search.city start -->
        <td>
          <%= search.city %>
        </td>
        <!-- Display search.city end -->

        <!-- Display search.start_date start -->
        <td>
          <%= search.start_date %>
        </td>
        <!-- Display search.start_date end -->

        <!-- Display search.end_date start -->
        <td>
          <%= search.end_date %>
        </td>
        <!-- Display search.end_date end -->

        <!-- Display search.created_at start -->
        <td>
          <%= time_ago_in_words(search.created_at) %> ago
        </td>
        <!-- Display search.created_at end -->

        <!-- Display search.updated_at start -->
        <td>
          <%= time_ago_in_words(search.updated_at) %> ago
        </td>
        <!-- Display search.updated_at end -->

        <td>
          <a href="/searches/<%= search.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @searches, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
